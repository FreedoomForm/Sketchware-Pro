# Final Audit Evidence Register

This register is intentionally incomplete until the migration is complete. A blank evidence field is a failed release gate, not an implicit approval.

| Capability domain | Stable source | Tier | Import evidence | Behavior test | Recovery/native provenance | Status |
|---|---|---|---|---|---|---|
| Legacy views 0–48 | `ViewBean`, `ViewBeans` | **R1 only** via `CreatorLegacyViewCapabilityMatrix` and `CreatorRuntimeWidgetCatalog` | `CreatorLegacyViewImporter` maps every inventoried type to a typed runtime widget | Exhaustive 49-type import regression, matrix tests, and full JVM suite pass; device interaction coverage is pending because this sandbox has no Android emulator binary or configured virtual device, and KVM acceleration is unavailable | No native fallback is permitted; unsupported rendering is blocked as R0 | **Open** — device behavior parity is not yet evidenced |
| Block and event models | `BlockBean`, `EventBean` | **R1 subset / visible R0 remainder** | `CreatorLegacyArtifactImporter` imports supported view events, linked chains, and persisted `if_state_equals` substacks into `CreatorEventBinding`; `CreatorRuntimeBlock` has typed nested state-equality branches | Importer tests cover typed message blocks, linked chains, supported conditional substacks, and rejection of `executeJava`; control-flow execution, nested persistence, focused regressions, and the full JVM suite pass | Unsupported opcodes are reported as R0 and never delegated to code generation | **Open** — legacy opcode and activity-event coverage remains incomplete |
| Component models | `ComponentBean` | **R1 only**; all 30 types map through `CreatorRuntimeComponentServiceMatrix` | Component descriptors persist in `legacy.components` and bind to reviewed runtime service IDs | Component matrix, dispatcher, service compilation, and permission-status tests pass | R2/R3 plugin and native-build tiers removed from the active runtime path | **Open** — device/configuration behavior tests remain incomplete for hardware, Firebase, and Ads |
| Project/library/resource models | `ProjectFileBean`, `ProjectLibraryBean`, `ProjectResourceBean` | **R1 metadata subset / visible R0 remainder** | `CreatorLegacyArtifactImporter` persists project file, enabled integration library, and resource metadata into the versioned Project IR; `CreatorRuntimeResourceResolver` resolves path-safe project image files for live image widgets | Importer regressions cover activity metadata, Firebase-library configuration, resource identity, explicit blocking of arbitrary local/native libraries, and path-safe existing/missing/traversal image resolution; focused and full JVM suites pass | Arbitrary libraries remain visible R0 because they cannot execute as trusted runtime services | **Open** — styles, non-image resource families, and all project-model relationships remain incomplete |
| Build/export paths | Compiler and `ProjectBuilder` families | Not accepted as a runtime execution tier | `CreatorNativeBuildAdapter`, `CreatorNativeBuildQueue`, and their test were removed | Source audit verifies no remaining active Creator Runtime native-build classes or R2/R3 tier references | Creator Runtime live execution must not install a rebuilt APK | **Open** — portable resource import and end-to-end live-project audit remain incomplete |
| Editor and AI actions | Tool registry and editor actions | Runtime operation pipeline | N/A | User and AI operation regression coverage passes | Revision history and shake recovery remain transparent and host-protected | **Open** — full legacy editor-action import inventory remains incomplete |

## Release gate

The 100% declaration is permitted only when every `Open` row has been decomposed into capability-level records with passing import and behavior evidence. The final audit must contain **zero R0 entries** and **zero R2/R3 execution paths**. An unsupported item must remain a visible R0 exception; it cannot be omitted from the denominator.

## Incremental evidence

`CreatorRuntimeComponentServiceMatrixTest` verifies that all 30 `ComponentBean` identifiers bind to an explicit R1 runtime service. `CreatorLegacyViewCapabilityMatrixTest` verifies that every inventoried legacy view type resolves to R1, and `CreatorLegacyViewImporterTest` imports all 49 view types into the typed runtime document. `CreatorLegacyArtifactImporterTest` demonstrates import of a component descriptor plus view event block and verifies that executable legacy Java is rejected instead of being sent to a plugin or native-build fallback. `CreatorProjectDocumentCodecTest` verifies imported service descriptors and typed event blocks survive document serialization. The full `:app:testDebugUnitTest` suite passes. These are JVM-level evidence only; they do **not** close the device behavior, project/library/resource, or legacy build-removal gates.
