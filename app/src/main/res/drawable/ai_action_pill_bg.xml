<?xml version="1.0" encoding="utf-8"?>
<!-- Rounded background for the floating action pill (Fetch / Add Model)
     on the provider detail Models tab. -->
<shape xmlns:android="http://schemas.android.com/apk/res/android"
    android:shape="rectangle">
    <solid android:color="@color/ai_chat_surface" />
    <corners android:radius="24dp" />
    <stroke android:width="1dp" android:color="@color/ai_chat_border" />
</shape>
