<?xml version="1.0" encoding="utf-8"?>
<!-- Selector for a single segment (Act or Plan) inside the segmented toggle.
     Active state = accent background (selected segment),
     Inactive state = transparent (lets the container's track show through). -->
<selector xmlns:android="http://schemas.android.com/apk/res/android">
    <item android:state_selected="true">
        <shape android:shape="rectangle">
            <solid android:color="@color/ai_chat_accent" />
            <corners android:radius="12dp" />
        </shape>
    </item>
    <item>
        <shape android:shape="rectangle">
            <solid android:color="@android:color/transparent" />
            <corners android:radius="12dp" />
        </shape>
    </item>
</selector>
