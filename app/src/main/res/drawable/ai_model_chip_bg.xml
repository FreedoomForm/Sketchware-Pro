<?xml version="1.0" encoding="utf-8"?>
<!-- Background for the small chip that shows the current model name in the
     chat input bar (model selector button). -->
<shape xmlns:android="http://schemas.android.com/apk/res/android"
    android:shape="rectangle">
    <solid android:color="@color/ai_model_chip_bg" />
    <corners android:radius="14dp" />
</shape>
